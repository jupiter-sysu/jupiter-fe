// This reducer is useless, and just to initilize the redux setup.

const initState = [{ id: 0 }];

export default function reducer(state = initState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
